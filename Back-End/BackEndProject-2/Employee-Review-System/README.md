1 = first use the command: (npm install) for installing all the dependencies:
2 = second use the command: (npm start)  for running the application: 
3 = third we need to use the (localhost:8080) on the browser for  running this application on the browser:

IMP things:
1 = (admin-Id) = harishpathania150@gmail.com:
2 =  (admin-password) = 12345:
